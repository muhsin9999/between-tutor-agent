# Brand assets

Generated 2026-09-12 with Higgsfield (`gpt_image_2_5`), sized for Telegram.

| File | Size | Used for |
|---|---|---|
| `bot-avatar.png` | 512×512 | BotFather `/setuserpic` |
| `miniapp-card.png` | 640×360 | BotFather `/newapp` photo — **must be exactly this size** |

The mark: two navy bars are the two lessons; the six amber dots between them are
the days the product exists to fill. No text — a wordmark is unreadable at 40px
in a chat list, which is where this is actually seen.

Palette: ink navy `#16306B` · amber `#F5A623` · cream `#FBF5E6`.
Reuse it in the panel so the Mini App and the bot read as one product.
